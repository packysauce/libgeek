GEEKTOOLDIR=~/.geektool
IMGDIR=$GEEKTOOLDIR/circle_percent_images

percent=$(vm_stat | grep -v Mach | head -n 5 | perl -ne 'm/(\d+)/; print "$1\n";' | xargs ./memcalc)
cp $IMGDIR/$percent.png $IMGDIR/current.png
