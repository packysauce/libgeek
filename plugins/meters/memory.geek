
. $LIBGEEK/libgeek

MYDIR=`getPluginDir meters/memory`

percent=$(vm_stat | grep -v Mach | head -n 5 | perl -ne 'm/(\d+)/; print "$1\n";' | xargs $MYDIR/bin/memcalc)
setPercentImage "memory-meter" $percent
